# Production Environment Configuration
# Copy to .env.production and adjust as needed:
#   cp .env.production.example .env.production

NODE_ENV=production
PORT=3001

# Metrics collection interval in milliseconds (default: 60 seconds)
METRICS_COLLECTION_INTERVAL_MS=60000

# How long to retain historical metrics in hours (default: 30 days)
METRICS_RETENTION_HOURS=720

# Comma-separated list of systemd services to monitor
MONITORED_SERVICES=homelab-observability,docker,ollama,nginx,postgresql,redis,ssh
